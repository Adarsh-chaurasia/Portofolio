/** @type {import('next').NextConfig} */
const nextConfig = {
    output : "export",
    reactStrictMode: true,
    images: {
        domains: ['images.unsplash.com']
    },

    
    
};

export default nextConfig;
